#' @param zeroDiag Boolean flag, whether the diagonal should be filled with zeroes or with feature entropies. 
#' @return A numerical matrix with scores, with row and column names copied from \code{X}.
